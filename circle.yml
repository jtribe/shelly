#
# Build configuration for Circle CI
#
dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter tool,extra-android-m2repository,extra-android-support,extra-google-m2repository,android-24
    - echo y | android update sdk --no-ui --all --filter build-tools-23.0.3
machine:
    java:
      version: oraclejdk8
test:
    override:
        - ./gradlew test
    post:
        - find shelly/build -name 'TEST-*.xml' -exec cp '{}' $CIRCLE_TEST_REPORTS \;
general:
  artifacts:
    - shelly/build/outputs
    - shelly/build/reports